1. 배열의 선언과 사용
- int a[10]; //선언
  float b[100]; //선언

2. C array의 특징
- index(첨자)는 0부터 시작
- 선언에서의 첨자는 총 갯수
- 즉 int a[100]; 으로 선언하면
  a[0], a[1], ... a[99] 까지임
- 첨자가 범위를 넘어도 문법적으로는 하자 없으나
  runtime error 발생

3. 세그멘테이션 오류
- 배열 index값이 정상 범위를 초과했을때
- scanf 함수에서 변수명 앞에 &가 빠졌을때

